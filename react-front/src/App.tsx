import React from 'react';
import TodosList from '../src/components/TodoList';

const App: React.FC = () => {
    return (
        <div>
            <TodosList />
        </div>
    );
};

export default App;
